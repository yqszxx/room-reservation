{% extends 'base.html.twig' %}

{% block title %}
    {% trans with {'%room%' : room.name} %}Reserve %room%{% endtrans %}
{% endblock %}

{% block body %}
    <h3 class="text-center">{% trans with {'%room%' : room.name} %}Reserve %room%{% endtrans %}</h3>
    <div class="row justify-content-center">
        <div class="col-4">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}

